const listaAlunos = ['José', 'Armando', 'Pelé'];
const grupo1 = listaAlunos.slice(0, listaAlunos.length / 2);
const grupo2 = listaAlunos.slice(listaAlunos.length / 2);
console.log(grupo1);
console.log(grupo2);